<html>
    <head>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">

        <style>
        
            .dateimpression{
                text-align:right;
                
            }

            .spandate{
                background-color: rgb(187, 189, 191);
            }
        
        
        </style>
    <head>

    <body>
        <center>
            <h3>Liste des Opérations du {{ datedebut | date('d-m-Y') }} au {{ datefin | date('d-m-Y')}}</h3>
        </center>
        <br/>
        <br/>
        <div class="dateimpression">Date impression:<span class="spandate">{{"now" |date('d-m-Y')}}</span></div>

        <br/><br/><br/>

        {% for op in operations  %}
          <div>
           <hr>
        <p style="text-align:left;" >Date: {{ op.DATE_MOUVEMENT | date("d-m-Y") }}<span style="float:right;" ><stron>{{op.MONTANT | number_format(0,'','.')}} F CFA</span></strong> </p>
        <h3>{{op.SOUSRUBRIQUE}}</h3>
        <p>{{op.COMMENTAIRE}} </p> 
        </div>
       
        {% endfor %}
      










    </body>






</html>
